import logo from "../assets/logo.png";

const Logo = () => {
  return (
    <div style={{ marginBottom: "75px", marginTop: "55px" }}>
      <img src={logo} alt="Weather App - Logo" />
    </div>
  );
};

export default Logo;
